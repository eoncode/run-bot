# UI Test Runner Setup

One-time manual step required on the self-hosted Mac before UI tests will run.

## Grant Accessibility permission to xcodebuild

**System Settings → Privacy & Security → Accessibility** → add `/usr/bin/xcodebuild`
